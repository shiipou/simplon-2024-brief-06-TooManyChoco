export async function createEvent(event_date, choix, anonyme, username) {
    console.log(choix);
    return await console.log("ce qui est envoyé : " + event_date + " " + choix + " " + anonyme + " " + username);
    }